# steg-scripts

Run Claude CLI in a loop until a task is complete.

## Local Development

Link the package globally to use the `ralph` command:

```bash
npm run link
```

Verify it works:

```bash
ralph --help
```

To unlink:

```bash
npm run unlink
```
